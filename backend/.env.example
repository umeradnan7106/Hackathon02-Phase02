# Backend Environment Variables Template
# Copy this file to .env and fill in the values

# Neon PostgreSQL Connection String
# Get from: https://neon.tech/app/projects
# Format: postgresql://user:password@ep-xxxxx.neon.tech/dbname?sslmode=require
DATABASE_URL=

# JWT Secret Key
# MUST match frontend BETTER_AUTH_SECRET (same exact value!)
# Generate with: openssl rand -base64 32
# OR PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# MUST be 32+ characters
BETTER_AUTH_SECRET=

# CORS Allowed Origins (comma-separated)
# Development: http://localhost:3000
# Production: Add your Vercel deployment URL (e.g., http://localhost:3000,https://yourapp.vercel.app)
CORS_ORIGINS=http://localhost:3000
